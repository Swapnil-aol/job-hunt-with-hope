# Composition — academic pack · Meera Raghavan

Pack row: [`../../SPEC-role-packs.md`](../../SPEC-role-packs.md) §3 **academic**. Pack proposes, Meera disposes — everything below passed the offer MCQ (spec §2).

## Apps enabled

| App | Layout · class | Content for Meera |
|---|---|---|
| Selected publications | `list` · professional — **lead app, default-open (R9)** | 23 entries, venue + year + citation count in mono, sorted reverse-chronological; "under review ×3" pipeline block at top, labeled honestly |
| CV download | contact-line anchor | One-click PDF; the pack renders ORCID prominently in the contact line (cross-pack canonical-platform rule, spec §4) |

No informal apps — pack default. No quote-wall (trust-economy roles only, spec §4).

## Above-the-fold order (per the demand-side scan)

Committees spend ~1 minute per file: tailoring check → CV → only what was requested. So:

1. **Hero**: name · "Computational linguist — low-resource MT" · current role · ORCID + CV link
2. **Selected publications** (default-open): pipeline first, then 2 ACL orals pinned
3. **Education** → **Experience** (teaching evals inline: 4.7/5, n=212) → **Skills**

## Demand-side fidelity

- **Shortlist signals honored**: active pipeline momentum (3 under review, named); teaching evidence *with* evaluations (PGCert + eval numbers); a complete file — completeness over curation, this pack only.
- **Bin signals avoided**: no generic boilerplate in the hero (her subfield is named); no unrequested extras (no side-quests app, no gallery); no Google Scholar harvest (DO-NOT-FETCH — OpenAlex/ORCID instead).

## Extrapolation notes — what Hope generalizes vs what is Meera's

1. **Generalizes**: publications-list-first emphasis, citation counts in mono, ORCID in the contact line, conservative register, CV link prominence — every academic gets these.
2. **Generalizes**: the "completeness over curation" inversion — Hope must *not* apply other packs' 3–5-item caps here.
3. **Meera-specific**: the pipeline block at the top of the list (early-career signal — pipeline trajectory). A senior academic leads with the established record + service/leadership instead (spec §3 modulation column).
4. **Meera-specific**: Sabda's GitHub stars on the timeline — only academics with a genuine software footprint get an OSS metric; Hope must not manufacture one.
5. **Meera-specific**: the SSHRC grant as a timeline project. Grants are pane-mapped to projects only when the user confirms co-PI-level ownership.
